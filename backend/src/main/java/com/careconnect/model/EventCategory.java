package com.careconnect.model;

/**
 * Mirrors the `category` ENUM on the `events` table.
 */
public enum EventCategory {
    BIRTHDAY,
    FESTIVAL,
    VISITING_DAY,
    FUNDRAISER,
    MEDICAL_CAMP,
    OTHER
}
